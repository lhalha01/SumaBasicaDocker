# proxy.py — API Reference

!!! info "Generación automática"
    Esta documentación es generada automáticamente con [pdoc](https://pdoc.dev) en cada ejecución de la pipeline CI/CD.  
    El contenido que ves aquí es un **placeholder** que se reemplaza durante la stage `DocsGenerate`.

!!! warning "Contenido pendiente"
    Para ver la API Reference completa, ejecuta la pipeline o construye la imagen docs localmente:
    ```bash
    pdoc proxy --format markdown > docs/api/proxy.md
    ```
